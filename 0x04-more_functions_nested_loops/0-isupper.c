#include "main.h"
/**
* _isupper - checks for uppercase characte
*
* @c: Variable text
*
* Return: 1 (Success)
*
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
