#include "main.h"

/**
 * print_alphabet - main function that prints the alphabet, in lowercase
 * Return: Always 0
 */

void print_alphabet(void)
{
char v;
for (v = 'a'; v <= 'z'; v++)
_putchar(v);
_putchar('\n');
}
